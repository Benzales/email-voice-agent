[project]
name = "email-voice-agent"
version = "0.1.0"
description = "Voice-driven email agent with Gmail MCP integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-generativeai>=0.8.3",
    "mcp>=1.12.1",
    "python-dotenv>=1.0.0",
]
