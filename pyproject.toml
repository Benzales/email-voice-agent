[project]
name = "email-voice-agent"
version = "0.1.0"
description = "A next-generation, voice-driven email agent for hands-free email processing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai",
    "librosa",
    "soundfile"
]
