fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1
pydub==0.25.1

# Reuse existing project deps assumed present in root env:
# mcp_agent and google genai would be in the parent; backend container should install them too if needed

